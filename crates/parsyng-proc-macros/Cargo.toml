[package]
name = "parsyng-proc-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
parsyng-quote = { path = "../parsyng-quote" }
unicode-ident = "1"

[features]
default = ["bootstrap"]
bootstrap = []
