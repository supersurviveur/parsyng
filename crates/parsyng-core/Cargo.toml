[package]
name = "parsyng-core"
description = "Core components of `parsyng`"
version = "0.1.0"
edition = "2024"
license = "MIT"
readme = "README.md"
repository = "https://github.com/supersurviveur/parsyng"
keywords = ["macros", "proc", "quote"]
categories = ["development-tools::procedural-macro-helpers"]

[dependencies]
parsyng-quote-macros = { path = "../parsyng-quote-macros" }
unicode-ident = "1"

# Optional dependencies
proc-macro2 = { version = "1", optional = true }

[features]
proc-macro2 = ["dep:proc-macro2", "proc-macro2/span-locations"]
debug-pretty = []
